# This file configures the Replit IDE's "Run" button.
# All deployment is now intended to be done via the Dockerfile on a standard container platform.
entrypoint = "app.py"

[run]
command = ["python", "app.py"] 